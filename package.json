{
  "name": "keyv-nedb-store",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@biomejs/biome": "^2.3.8",
    "@types/bun": "latest",
    "standard-version": "^9.5.0"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@seald-io/nedb": "^4.1.2",
    "keyv": "^5.5.5"
  },
  "scripts": {
    "build": "tsc",
    "fmt": "biome check --fix --unsafe",
    "prepack": "bun fmt && bun run build",
    "release": "standard-version && git push --follow-tags && npm publish"
  },
  "version": "0.0.1"
}
